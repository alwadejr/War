define(function (require) {

    return function () {

        var bootstrap = require("bootstrap"),
            firebase = require("firebase"),
            hbs = require("hbs"),
            q = require("q"),
            getDeck = require("get-deck"),
            gameplay = require("gameplay")

    };
});